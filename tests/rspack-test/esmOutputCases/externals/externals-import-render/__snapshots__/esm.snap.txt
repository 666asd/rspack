```mjs title=main.mjs
import fs_0 from "fs";
import { join as external_path_join, resolve as external_path_resolve } from "path";

// fs

// path

// ./index.js




it('should compile', () => {
	console.log.bind([fs_0, external_path_join, external_path_resolve])
})

export {};

```